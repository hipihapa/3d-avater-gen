import 'package:flutter/material.dart';

class EditAvatarScreen extends StatelessWidget {
  const EditAvatarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text('Edit Avatar Screen'),
        ],
      ),
    );
  }
}